# Critical Minerals

Static visual explainer starter based on the `ve-designsys` patterns.

Open `index.html` directly in a browser to review the current page. The placeholder media and mock map-scrolly sections
are ready to be replaced with production assets and Mapbox implementation details.
